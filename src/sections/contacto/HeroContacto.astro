---
import { getImage } from "astro:assets";
import imagen_contacto from "../../assets/image_contacto.jpg";
const heroImage = await getImage({ src: imagen_contacto });

/* console.log(heroImage); */
---

<section
    class="flex flex-col justify-center items-center h-screen bg-cover text-white"
    style=`background-image: url(${heroImage.src});`
>
    <article class="w-3/4">
        <h1 class="text-4xl md:text-6xl font-extrabold">Contacto</h1>
        <p class="md:text-xl">contactanos</p>
    </article>
</section>
